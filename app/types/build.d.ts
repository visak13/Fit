/// <reference types="vite/client" />

/**
 * The source stamp of the build this bundle came from, substituted at build time by
 * `vite.config.ts`. See `tools/source-stamp.mjs` for what it is a hash of and why it exists.
 */
declare const __BUILD_STAMP__: string;
